import type { LumberroomConfig, RuntimeState } from "./types.js";

export function createState(cfg: LumberroomConfig | null, inertReason: string | null, stateDir: string): RuntimeState {
  throw new Error("T4");
}
